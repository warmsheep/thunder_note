# 闪记完整开发计划

## 文档定位
本文档是闪记项目后续开发工作的总计划文档，用于长期维护整个项目从 **MVP → 增强版 → 完整项目 → 远期扩展** 的推进路线。所有开发任务、模块边界、接口级 TODO、阶段依赖与验收口径，后续都以本文件为主维护。

本文档严格区分四类信息：
- **当前已实现**：代码中已存在并可核对
- **当前部分实现**：代码中已有骨架或局部能力，但未形成闭环
- **当前主线必补**：为了形成可用产品主线，必须尽快完成
- **目标态规划**：方向已明确，但不属于当前主线交付范围

## 计划使用规则
- 每个任务都应尽量标注：**状态**、**来源**、**依赖**、**验收出口**。
- 事实来源优先级遵循根目录 `AGENTS.md`：代码/迁移脚本 > 当前态文档 > 目标态文档 > HTML 原型。
- 如果某条 TODO 会影响接口、数据结构、跨端行为，必须先补充“当前事实”再推进开发。
- 当前主线只围绕 **Spring Boot 后端 + Android 客户端**，Web / iOS / admin 一律放到远期阶段。
- 本文档是唯一执行型 backlog；`TODO事项.md` 只作为路线图摘要，不再与本文件并行维护详细执行优先级。

## 模块工作包模板
后续新增模块或补充计划时，尽量按下面结构追加：
- 目标
- 当前事实
- 后端接口
- 数据库/迁移
- Android domain/data/presentation
- 同步影响
- 前置依赖
- 验收标准
- 所属阶段（MVP / 增强版 / 完整项目 / 远期）

---

## 一、项目目标

### 1.1 产品目标
闪记希望提供一套围绕“短笔记 + 对话消息 + 合集整理 + 多端同步”展开的个人知识记录体验。最终目标不是单一笔记应用，而是一个支持多端协同、内容沉淀、轻量沟通式记录与结构化整理的知识管理产品。

### 1.2 当前现实
当前仓库真实落地内容包括：
- Spring Boot 后端
- Android Java 客户端（当前主模块）
- HTML 原型
- 中文设计与规划文档

当前尚未落地代码的部分包括：
- Web 用户端
- iOS 客户端
- 管理后台
- 搜索、收藏、完整同步协议、完整多媒体消息体系

### 1.3 当前总原则
当前阶段是"在 Java 客户端已经接管主模块的前提下，继续补齐核心页面、数据链路和后端真实联调"。后端仍继续作为真实事实来源。

### 1.4 Android 客户端决策
- Android 当前 `thunder-note-android/` 为 **Java 客户端主模块**。

---

## 二、开发阶段总览

## 阶段 A：MVP（后端稳定 + Android Java 客户端补齐）
目标：在现有 Java Android 客户端基础上补齐核心页面、后端真实链路和主要交互闭环，让新主线从“可编译壳层”进入“可持续开发主链”。

### MVP 验收口径
- Java 客户端已稳定构建并成为仓库唯一 Android 主模块
- Auth / FlashNote / Chat 具备可运行主链，不再只是静态页面壳层
- Collection / Favorite / Profile 的页面、数据来源与成熟度有明确状态说明
- Android ↔ 后端新的执行型联调基线开始以 Java 客户端为准持续维护

### MVP 进入条件
- 当前态文档已经完成与代码事实对齐
- 历史漂移（`/api/v1`、邮箱登录、MySQL、未落地端）已在文档中显式标记

### MVP 完成检查清单（Definition of Done）
- [x] 已明确哪些知识必须继承到 Java 客户端：接口事实、产品页面范围、本地能力边界、经验库问题
- [x] `docs/完整开发计划.md`、`TODO事项.md`、`客户端架构设计.md`、`架构设计.md`、`技术栈选型.md`、`开发测试部署经验库.md` 已同步回写

### MVP 阶段出口
- 只有当上面的完成检查清单全部满足，才允许进入增强版阶段。

## 阶段 B：增强版（主线能力补齐）
目标：补齐收藏、文件闭环、多媒体消息基础链路、同步可靠性、用户体验完善。

### 增强版进入条件
- MVP 阶段出口已满足
- 当前主线数据模型与接口契约已稳定，不再频繁重命名或改语义

### 增强版阶段出口
- 收藏能力具备完整后端 + Android 真实链路
- 文件链路具备基础元数据治理与稳定错误处理
- 多媒体消息至少完成一类真实扩展能力（建议图片）
- 同步具备基础重试、待同步队列、失败场景处理
- 核心主线拥有最小可用测试与联调检查清单

## 阶段 C：完整项目（产品能力完善）
目标：补齐搜索、完整同步协议、系统信息、更多内容组织能力，并把文档、测试、运维能力拉齐。

### 完整项目进入条件
- 增强版阶段出口已满足
- auth / flashnote / message / collection / file / sync 主线已经稳定，不再依赖大量模拟数据

### 完整项目阶段出口
- 搜索、完整同步协议、系统与诊断能力均有真实实现与文档支撑
- 工程化、测试、发布与运维文档形成可执行体系
- 当前态与目标态文档分界清晰，阶段内无重大漂移未处理

## 阶段 D：远期扩展（多端与后台）
目标：推进 Web、iOS、管理后台，以及更高级的搜索、内容治理、系统运营能力。

### 远期扩展进入条件
- 完整项目阶段出口已满足
- 当前后端 API 契约与核心数据模型已足够稳定，可被新端复用

### 远期扩展阶段出口
- Web / iOS / admin 至少具备独立规划、独立任务分解与与当前主线对齐的数据契约

---

## 三、模块地图

### 3.1 当前后端模块状态
| 模块 | 状态 | 说明 |
|------|------|------|
| auth | 当前已实现基础 | 注册、登录、刷新、登出；已补“活跃会话自动续期 + 绝对过期上限（30天）”策略 |
| user | 当前已实现基础 | 获取/更新 profile；已补联系人列表/好友请求/联系人搜索/删除联系人能力（好友关系由 `friend_relations` 承载）；联系人列表现额外返回 `relationStatus/latestMessage`，并纳入“等待对方同意”的挂起联系人 |
| flashnote | 当前已实现基础 | 基础 CRUD，当前已补 `icon` 字段并以 `tags` 承接单一合集归属；已补 `pinned/hidden/inbox` 扩展状态与“收集箱（虚拟闪记ID=-1）” |
| message | 当前已实现基础 | 列表、发送、SSE stream、删除、消息总数统计，已补 `flash_note_id` / `role` 承接 Android 闪记内对话，并支持按 `peerUserId` 拉取联系人双向会话 |
| collection | 当前已实现基础 | 基础 CRUD，当前作为合集目录，不含独立合集-闪记关系表 |
| file | 当前已实现基础 | 上传（支持500MB）、下载、返回原始文件名；已修复 `InputStreamResource` 导致的下载流二次读取问题，下载响应现补齐 `Content-Type` / `Content-Length` / `inline` header，兼容 Android Glide/ExoPlayer 展示 |
| sync | 当前已实现基础 | bootstrap / pull / push 已可返回真实聚合结果，push 可处理 notes / collections / messages |
| favorite | 当前已实现基础 | 后端与 Android 已接通基础消息收藏链路，收藏列表返回闪记图标；收集箱收藏项会回填 `收集箱/📥` 标题与图标 |
| search | 当前已实现基础 | 后端消息搜索+上下文已实现，并补 `noteMatched` 用于区分闪记命中与消息命中；Android 已实现内嵌实时搜索 |
| system | 未实现 | 只有规划 |
| admin | 未实现 | 只有规划 |

### 3.2 当前 Android 模块状态（Java 客户端主线）
| 模块 | 状态 | 说明 |
|------|------|------|
| auth | 当前已实现基础 | Java 版 Splash / Login / Register 已接通真实 auth repository、token 持久化与 refresh 契约 |
| flashnote | 当前已实现基础 | Java 版创建/编辑已改为图标 + 名称 + 合集归属，支持创建/编辑/删除（级联删除消息）、左滑删除（固定内容不位移，右侧显示删除块并点击后再二次确认）、进入聊天。列表首行已统一为“闪记名称 + 灰色 `#合集名称`”，次行展示后端聚合的最新一条消息（媒体类型统一用 `[图片]/[视频]/[语音]/[文件]` 占位） |
| message/chat | 当前已实现基础 | Java 版聊天页已接真实消息接口，支持富媒体消息、文件缓存预览、视频缓存播放、真实用户头像、分页加载（默认 20 条+上拉翻页防抖）、搜索跳转高亮。已补消息长按“分享”能力（图片/视频/语音/文件可通过 FileProvider 分享到外部 App，优先使用原始文件名），并增强底部定位补偿（混合图文场景连续校准到底部）。录音弹层已改为与语音播放一致的声纹动画风格；Markdown 文本消息现可按格式渲染显示。2026-03-25 已补 Glide→OkHttp 认证链路统一，并通过一次性清理旧 Glide 脏缓存修复重新安装后的聊天图片/头像空白问题 |
| collection | 当前已实现基础 | Java 版合集页默认展示“合集（分类）-闪记”层级，当前归属通过 `flash_notes.tags` 表达；已移除搜索按钮，使用矢量图标（编辑/删除）。编辑/删除按钮已改用 bindingAdapterPosition 实时取数据；收集箱不再作为单独合集分组，而是固定出现在“未分类”组首位 |
| favorite | 当前已实现基础 | Java 版收藏页已接真实收藏列表，显示闪记实际图标（顶部对齐），可从聊天页长按消息加入收藏。收藏列表已支持媒体预览（图片/视频/文件/语音）与闪记卡片消息预览；卡片消息现按聊天内卡片样式展示并可进入详情。长按任意区域（空白区/媒体/卡片）弹出悬浮菜单，包含"复制"和"删除"选项，样式与聊天消息长按菜单一致 |
| contact | 当前已实现基础 | Java 主壳已新增“联系人”底部 tab；现支持“联系人/新的好友请求”双视图、请求红点、滑动删除联系人（带红色背景与垃圾桶动画）、搜索用户并发起好友请求、联系人会话（文字/语音/图片/视频/文件）；联系人列表会显示最新消息与挂起申请状态；滑动删除时根据状态显示"删除"/"取消"覆盖层，点击后弹出确认框；搜索UI已改为底部搜索框（参考闪记搜索交互），支持实时搜索与回车键触发，移除顶部刷新按钮并改为切换页面自动刷新；联系人头像与聊天对方头像支持自定义图片URL（通过 Glide + MediaUrlResolver + 统一 OkHttp 鉴权链路加载）；好友请求空状态使用清晰灰色人物图标 |
| profile | 当前部分实现 | Java 版页面已提供真实头像、闪记数/收藏数/消息记录数统计、修改密码、调试日志；已修复头像裁剪后上传与显示链路（我的页+聊天页），并修复“图片头像切 emoji 后回显历史头像”的缓存问题；2026-03-25 已补“重新安装后头像图片空白”链路修复（统一 Glide/OkHttp 鉴权 + 升级时一次性清理旧媒体脏缓存）；入口从原头像点击进入“修改个人资料”（昵称/简介/头像）保存流程，当前资料保存接口也会持久化头像字段 |
| sync | 当前已实现基础 | Java 主链已接入 bootstrap / pull / push，并提供手动触发入口 |
| file | 当前已实现基础 | Java 主链已具备上传（FileUploadResult解析）、下载、本地缓存预览的闭环 |

### 3.3 目标态但当前无代码的模块
- Web 用户端
- iOS 客户端
- 管理后台
- 搜索
- 收藏能力增强（如跨类型收藏与同步联动）
- 完整同步冲突系统

### 3.4 横切工程能力
| 能力 | 状态 | 说明 |
|------|------|------|
| 自动化测试 | 当前部分实现 | 已补一批 message / favorite / sync 行为测试，但整体覆盖仍不足 |
| CI | 未实现 | 当前仓库未发现 CI 工作流 |
| 接口契约治理 | 当前部分实现 | 已开始收敛文档，但 Android/后端仍有漂移 |
| 异常与日志治理 | 当前部分实现 | 后端有全局异常处理，并已补统一 API 请求/响应日志（接口名 + 请求参数 + 响应参数）；MyBatis SQL stdout 已临时关闭，当前仍缺结构化审计、链路追踪与指标化观测 |
| 发布与环境管理 | 当前部分实现 | 本地配置存在，缺明确 staging/prod 流程 |
| 文档维护机制 | 当前部分实现 | 已清理核心文档，但需持续维护节奏 |

### 3.5 Android 聊天 / 上传 / 闪记列表数据流重构专项（新增执行主线）

> **定位**：这是面向后续新会话直接执行的专项 backlog，用于收敛 Android 当前“聊天空白、上传状态丢失、闪记列表重复请求”的系统性问题。
>
> **背景结论（2026-03-23）**：当前 Android 客户端在 `chat/message + flashnote list + upload/sync` 这条链路上，存在明显的数据流层架构缺陷：
> - 聊天消息与上传中状态仍主要依赖内存 `LiveData + HashMap`，不是稳定的本地单一事实源；
> - `MessageRepositoryImpl.bindFlashNote()/bindContact()` 先清空列表再拉远端，容易制造空白聊天；
> - `FlashNoteViewModel` 构造刷新 + `FlashNoteTabFragment.onResume()` 刷新 + `MainShellFragment.replace()` 重建，共同造成闪记列表频繁请求；
> - 当前 sync/file 仍是 MVP 最小闭环，不足以支撑“离开页面后仍稳定显示上传中、回到页面继续可见”的产品预期。
>
> **执行原则**：
> 1. 先止血，再重构，再做真正离线优先；
> 2. 每一阶段都必须可独立验收，不允许跨阶段混改导致回归面失控；
> 3. 所有改动优先围绕 `thunder-note-android/` 收敛，除非后端接口契约明确不够用；
> 4. 每完成一个阶段，必须同步回写本文件与《开发测试部署经验库》。 

#### 3.5.1 当前已确认的根因清单（作为后续会话前置事实）
- `thunder-note-android/app/src/main/java/com/flashnote/java/data/repository/MessageRepositoryImpl.java`
  - 当前用 `Map<Long, MutableLiveData<List<Message>>> conversations` 承载消息主链，仍是**内存态**。
  - 历史问题：`bindFlashNote()` / `bindContact()` 曾先 `setValue(new ArrayList<>())` 再 `loadMessages()`，是“聊天空白”的第一直接根因；**2026-03-23 已完成止血修复**，当前已改为“有缓存消息则不清空，只在无缓存时首次加载”。
- `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatViewModel.java`
  - 历史问题：发送/上传链路曾依赖当前会话上下文，异步回调可能串到新聊天；**2026-03-23 已完成一轮止血修复**，当前媒体发送优先使用显式目标会话 / `Message` 自带目标字段。
- `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatFragment.java`
  - Fragment 生命周期与上传/发送回调仍有耦合，虽然已做一轮 `isAdded()` 风险收敛，但结构上仍不是后台可持续状态机。
  - 历史问题：聊天页曾使用 `Activity` 级共享 `ChatViewModel`，导致不同聊天页共享当前会话状态；**2026-03-23 已完成止血修复**，当前已改为 `Fragment` 级 `ChatViewModel` 隔离实例。
- `thunder-note-android/app/src/main/java/com/flashnote/java/ui/main/FlashNoteViewModel.java`
  - 历史问题：构造函数内自动 `refresh()` 会放大闪记列表重复请求；**2026-03-23 已完成止血修复**，当前改为 `refreshIfNeeded()`。
- `thunder-note-android/app/src/main/java/com/flashnote/java/ui/main/FlashNoteTabFragment.java`
  - 历史问题：`onResume()` 无条件 `refresh()` 会导致 idle 场景重复请求；**2026-03-23 已完成止血修复**，当前改为 `refreshIfNeeded()`。
  - 历史问题：从闪记列表悬浮 `+` 发起上传后，若立刻进入并退出收集箱，上传完成/失败回调可能因 Fragment 生命周期检查提前返回，导致本地占位消息长期停留在 `uploading=true`；**2026-03-24 已完成止血修复**，当前上传结果回写已与页面 UI 存活解耦，失败时会清理本地假占位。
- `thunder-note-android/app/src/main/java/com/flashnote/java/ui/main/MainShellFragment.java`
  - 当前 tab 切换采用 `replace()`，会触发 Fragment 重建，放大列表刷新问题。
- `thunder-note-android/app/src/main/java/com/flashnote/java/data/repository/SyncRepositoryImpl.java`
  - 当前仍适合作为“调试/最小闭环同步”，不适合作为可靠离线优先执行器。

#### 3.5.2 阶段划分总览
| 阶段 | 目标 | 风险等级 | 是否允许直接开干 |
|------|------|----------|------------------|
| P0 | 止血修复：先消除空白聊天、上传状态丢失、闪记列表重复请求 | 高优先级 / 低至中改动 | 是 |
| P1 | 结构收敛：建立本地 pending/outbox 主链，收口上传/消息状态 | 高优先级 / 中改动 | 是，但必须在 P0 完成后 |
| P2 | 真正离线优先：把聊天/上传/sync 升级为本地单一事实源 + 后台同步 | 高优先级 / 中高改动 | 是，但必须在 P1 完成后 |

---

### 3.5.3 P0：止血修复（先停掉空白聊天与重复请求）

#### P0 目标
- 打开聊天页时，不再先出现空白消息列表。
- 大文件/大视频上传时，离开页面再回来，至少还能看到本地消息项与上传中的状态占位。
- 停留在闪记列表页时，不再因为 Fragment/ViewModel 生命周期导致持续重复请求。
- 本阶段**不追求完整离线优先**，只做止血和收口，不引入过大架构面。

#### P0 任务清单（必须按顺序做）

> **进展快照（2026-03-23）**
> - P0-1 已完成：聊天绑定不再先清空列表。
> - P0-2 已完成：同会话重复 bind 已加防重，聊天页改为 `Fragment` 级 `ChatViewModel`。
> - P0-3 已完成第一轮止血：图片/视频/文件/语音上传完成后已改为使用显式目标会话发送，修复“上传视频切到新聊天后落到新聊天”的串窗问题。
> - P0-4 已完成第一轮止血：闪记列表由“构造刷新 + onResume 刷新”收口为 `refreshIfNeeded()`。
> - 2026-03-24 实测补证：上传切换聊天/切 tab 的串会话问题已确认解决；闪记列表 idle 重复请求问题已确认解决。
> - 2026-03-24 补充修复：从闪记列表悬浮 `+` 发起图片/文件上传后，若立刻进入并退出收集箱，上传不再因页面切换中断，且失败时不会留下持续旋转的假占位。
> - 2026-03-24 继续收口：`EditProfileFragment`、`ProfileTabFragment`、`CardEditorFragment` 已按“业务状态与 UI 生命周期解耦”修复；卡片保存改为异步派发后可立即返回；闪记列表悬浮 `+` 的媒体选择已从“仅图片”扩为“图片 + 视频”。
> - 2026-03-24 聊天页补充止血：`ChatFragment` 的图片/视频/文件/语音发送失败后，会主动清理本地乐观占位，且离页后不再直接依赖 `requireActivity().runOnUiThread(...)` 做 UI 收口。
> - P0-5 仍需继续补齐其余真机/手工回归与文档化项，但上述两项 P0 核心止血已补到实测证据。

##### P0-1. 去除聊天绑定时的“先清空再加载”
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/data/repository/MessageRepositoryImpl.java`
  - [x] 重写 `bindFlashNote(long flashNoteId)`：禁止在会话绑定开始时直接 `liveData.setValue(new ArrayList<>())`。
  - [x] 重写 `bindContact(long peerUserId)`：同样禁止先清空消息列表。
  - [x] 明确会话初始化分支：
    - [x] 若当前 key 没有任何已加载消息，则允许设置“首次加载中”状态；
    - [x] 若当前 key 已有消息或本地 pending 消息，禁止覆盖现有列表。
  - [x] 为“首次进入会话”和“重新进入已有会话”分开处理分页状态，不允许二者共用硬重置逻辑。

**QA：**
- [x] 进入已有消息的闪记聊天页时，不先闪成空白（基于代码路径与构建验证已完成止血收口）。
- [x] 上传过程中退出再进入，同会话列表不被先清空（2026-03-24 已完成真机回归确认）。

##### P0-2. 收口 ChatFragment / ChatViewModel 的会话绑定行为
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatFragment.java`
  - [x] 核对 `onViewCreated()` 内的 `bindFlashNote()/bindContact()` 调用时机。
  - [x] 禁止同一个会话在一次页面重建中重复绑定两次以上。
  - [x] 为会话绑定增加“当前 key 未变化则不重复 bind”的保护。
  - [x] 将聊天页 `ChatViewModel` 从 `Activity` 级共享实例改为 `Fragment` 级实例，避免不同聊天页共享当前会话状态。
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatViewModel.java`
  - [x] 明确当前会话 key 的设置时机。
  - [x] 禁止异步回调依赖一个可能已被切换的“当前会话”上下文（止血层面已完成，结构层面仍待 P1/P2 彻底收口）。

**QA：**
- [ ] 同一聊天页旋转/返回/重新进入时，不出现重复绑定导致的列表闪空（仍需真机手工回归确认）。

##### P0-3. 把发送/上传回调改成“显式目标会话”
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatViewModel.java`
  - [x] 新增或改造发送接口：发送文字/媒体时明确带 `flashNoteId` 或 `peerUserId`，不要在回调执行时临时读取易变的当前会话状态。
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/data/repository/MessageRepositoryImpl.java`
  - [x] 保持并复用 `sendMessage(...)` / `sendMessageToContact(...)` 这类显式目标接口，避免“回调回来时发到错误上下文”。
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatFragment.java`
  - [x] 所有上传成功后调用 `sendMedia(...)` 的地方，统一传显式会话目标。
  - [x] 上传失败/压缩失败时，主动清理当前会话里的本地乐观占位，避免留下假 `uploading` 消息。
  - [x] 图片/视频/文件/语音发送链路统一改为通过 `runIfUiAlive(...)` 保护 UI 副作用，避免离页后直接依赖 `requireActivity()`。

**QA：**
- [x] 上传大文件时切到别的聊天/别的 tab，再回来不会串会话（2026-03-24 已完成实测回归确认）。
- [x] 聊天页媒体发送失败后，不再留下假上传中的本地占位（2026-03-24 已完成代码 + 构建验证，待你常规手工补证）。

##### P0-4. 收口闪记列表自动刷新触发器
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/main/FlashNoteViewModel.java`
  - [x] 去掉构造函数中的自动 `repository.refresh()` / `collectionRepository.refresh()`。
  - [x] 改为显式初始化函数，由页面决定是否首次拉取。
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/main/FlashNoteTabFragment.java`
  - [x] 重写 `onResume()` 刷新策略：改为“无缓存首次进入才刷新”或“显式用户操作才刷新”，禁止无脑 `refresh()`。
  - [x] 至少收口快速收集成功回调与首次进入场景，确保非显式操作不重复刷新。
- [ ] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/main/MainShellFragment.java`
  - [ ] 审核 tab 切换 `replace()` 是否会放大刷新；若本阶段不改 show/hide，至少保证不再依赖构造刷新。

**QA：**
- [x] 停留在闪记列表页 1 分钟，不操作时不持续请求服务器（2026-03-24 已完成实测确认）。
- [ ] tab 来回切换时，请求次数显著下降且无功能缺失（仍需手工回归确认）。

##### P0-5. 建立最小回归验证
- [ ] Android 手工测试用例补到文档：
  - [x] “聊天页进入不空白”
- [x] “上传中退出页面再回来仍可见”（2026-03-24 已实测：退出收集箱不会中断上传；收集箱进出后不再留下持续旋转的假占位）
- [x] “闪记列表 idle 不持续请求”（2026-03-24 已实测确认）
- [x] “编辑资料 / 我的页头像上传中离开页面，不会卡住上传状态或崩溃”（2026-03-24 已实测确认）
- [x] “卡片消息保存后可立即返回，后台继续上传/发送”（2026-03-24 已实测确认）
- [x] “闪记列表悬浮 `+` 选择媒体时支持图片和视频”（2026-03-24 已完成代码 + 构建验证，待常规手工补证）
  - [x] “聊天页图片/视频/文件/语音发送失败后，不残留假上传占位且离页不崩”（2026-03-24 已完成代码 + 构建验证，待常规手工补证）
- [x] 运行验证命令：
  - [x] `cd thunder-note-android && ./gradlew assembleDebug`
  - [x] `cd thunder-note-android && ./gradlew test`
  - [ ] 如本轮改动涉及 UI 生命周期，补跑 `./gradlew lint`

#### P0 验收标准
- [x] 聊天页不再因会话重绑先清空消息列表（代码止血修复 + 构建验证已完成）。
- [x] 上传中的本地消息离开页面后再回来仍可见（P0 止血层面已完成真机回归；跨页面/跨重启稳定恢复仍待 P1 本地 pending/outbox 结构彻底保证）。
- [x] 闪记列表页不再无操作持续请求（2026-03-24 已完成实测确认）。
- [x] 本阶段未引入新的聊天串会话、分页失效、闪退问题（截至 2026-03-24 已完成本轮相关真机回归补证）。

---

### 3.5.4 P1：结构收敛（建立本地 pending / outbox 主链）

#### P1 目标
- 让“上传中/待发送”消息有稳定的本地状态来源，而不是仅依赖内存对象。
- 聊天页展示改为“远端消息 + 本地 pending/outbox 合并视图”。
- 即便离开页面，回来后仍能从本地恢复看到待上传/待发送项。

#### P1 实施边界（必须先统一认知）
- P1 **不是**“真正离线优先”，也**不是**“APP 杀进程后自动后台恢复上传/发送”；这些属于 P2。
- P1 只收口“未确认消息”这条链：
  - 已确认消息：继续沿用当前 `MessageRepositoryImpl` 的远端拉取 + 当前分页 + 当前会话缓存。
  - 未确认消息：新增一个最小本地 pending/outbox 事实源。
- P1 不做大爆炸重写，必须保持以下部分尽量不动：
  - `MessageRepositoryImpl` 的远端分页主链；
  - `ChatFragment` 的页面结构与基础交互；
  - `MessageAdapter` 的主体渲染结构；
  - 闪记列表与收藏/合集等非聊天主链页面。

#### P1 关键约束（实施红线）
- 一条用户发送意图只允许对应一条本地 pending 记录，所有上传/发送/失败/重试都按 `localId` 归位。
- 任何网络动作开始前，pending 记录必须已经落本地；否则切页/重建后无法恢复。
- Dispatcher 只能读取 pending 行内的 `flashNoteId / peerUserId / conversationKey`，不能依赖“当前打开的聊天页”。
- 远端 confirmed 消息与本地 pending/outbox 必须分源存储，不能继续把 pending 直接混写进远端 `conversations` 缓存源。
- UI 最终只能观察“远端 confirmed + 本地 pending”的合并结果；Fragment 不再直接维护发送状态机。

#### P1 推荐实施顺序（严格按顺序，不要跳步）
1. **P1-0 准备阶段**：统一会话 key / 目标模型、补最小本地存储基础设施。
2. **P1-1 文本消息先行**：先让 TEXT 进入 pending/outbox；不要一开始就带媒体。
3. **P1-2 图片/文件扩展**：引入 upload -> send 双阶段状态机。
4. **P1-3 语音/视频扩展**：最后再接最复杂的媒体链路。
5. **P1-4 失败恢复与最小回归矩阵**：单条重试、切页恢复、断网恢复。

#### P1-0. 准备阶段（不改聊天主流程，只补基础设施）
- [ ] 统一会话目标与 key 规则，新增一个可复用的统一工具（命名可调整，如 `ConversationTarget` / `ConversationKeyUtil`）：
  - [ ] 同时覆盖闪记会话、联系人会话、收集箱会话。
  - [ ] `ChatFragment`、`ChatViewModel`、`MessageRepositoryImpl`、后续 pending store / dispatcher 全部只能复用这一个规则。
  - [ ] 禁止继续在不同类里各自拼 `flashNoteId` / `-peerUserId` key。
- [ ] 在 Android 端引入最小可行本地存储：
  - [ ] 优先评估并接入 Room；若放弃 Room，必须在文档中明确说明为什么选择其他实现。
  - [ ] 当前仓库暂无 Room 依赖，需先补依赖与基础 Database 装配。
- [ ] 新增最小 pending 实体（命名可调整，但职责必须明确，建议先统一为 `PendingMessage`，不要同时拆两张表）：
  - [ ] `localId`
  - [ ] `conversationKey`
  - [ ] `flashNoteId / peerUserId`
  - [ ] `clientRequestId`（建议新增，用于后续去重/映射）
  - [ ] `mediaType`
  - [ ] `content`
  - [ ] `localFilePath`
  - [ ] `remoteUrl`
  - [ ] `status`（QUEUED / UPLOADING / UPLOADED / SENDING / FAILED / SENT）
  - [ ] `createdAt`
  - [ ] `errorMessage`
  - [ ] `attemptCount`
  - [ ] `serverMessageId`（可选，但建议预留）
- [ ] 新增 DAO / Repository 基础边界：
  - [ ] `PendingMessageDao`
  - [ ] `PendingMessageRepository`
  - [ ] `AppDatabase`（或等价装配点）
- [ ] 明确并固化字段边界：
  - [ ] `localFilePath` 与 `remoteUrl` 必须分开，不能继续把本地路径塞进 `mediaUrl` 万能字段。
  - [ ] pending store 不直接复用远端 DTO 作为数据库实体。

**建议涉及文件：**
- `thunder-note-android/app/build.gradle(.kts)`
- `thunder-note-android/app/src/main/java/com/flashnote/java/FlashNoteApp.java`
- `thunder-note-android/app/src/main/java/com/flashnote/java/data/model/`（新增 pending 实体）
- `thunder-note-android/app/src/main/java/com/flashnote/java/data/local/`（新增 DB / DAO，目录名可调整）
- `thunder-note-android/app/src/main/java/com/flashnote/java/data/repository/`

**QA：**
- [ ] 本地表结构能完整表示文本、图片、视频、语音、文件消息的 pending 状态。
- [ ] 会话 key 在闪记 / 联系人 / 收集箱三条主链上无冲突。
- [ ] `assembleDebug` 通过，且未触发聊天主链行为变化。

#### P1 任务清单（P0 完成后才能做）

##### P1-1. 文本消息先行：先让 TEXT 进入本地 pending/outbox
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatFragment.java`
  - [x] 文本发送入口不再直接调用“立即发送成功后再写 UI”的旧模式。
  - [x] Fragment 只负责把文本输入交给新的 enqueue 接口，不再自己维护本地假消息。
  - [x] 断网发送时不再弹网络错误 toast，失败反馈以下沉到消息行状态为准。
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/ChatViewModel.java`
  - [x] 已增加文本 pending / 单条重试入口。
  - [x] ViewModel 不再直接承担发送状态机，只负责把命令转到 repository。
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/data/repository/MessageRepositoryImpl.java`
  - [x] 已接入“先写本地 pending，再触发 dispatcher”的文本发送主链。
  - [x] 发送成功后，当前聊天页可立即看到正式消息，不需要杀 APP 或重新登录。
  - [x] 发送失败后，会把当前 pending 标记为 `FAILED` 并保留在聊天列表中。
  - [x] 已修复 pending 时间投影与当前会话排序时序问题，离线失败项会出现在当前聊天底部附近，而不是静默插进历史消息里。
- [x] `PendingMessageRepository / Dao`
  - [x] 已支持新增文本 pending。
  - [x] 已支持按 `localId` 更新状态。
  - [x] 已支持按会话查询当前 pending。

**当前阶段不要做：**
- [ ] 不要在 P1-1 同时接入图片/视频/语音/文件。
- [ ] 不要在 P1-1 就重写聊天 Adapter 的全部渲染分支。

**QA：**
- [x] 发送文本后立刻切出聊天页再回来，仍能从本地恢复看到发送中的文本（当前文本主链已具备基础能力）。
- [x] 断网发送文本后，消息保留为 `FAILED`，不会直接丢失。
- [x] 失败文本在聊天列表左侧显示重发图标，可单条重试。
- [x] 单条文本点击重试后可成功发送，成功后当前聊天页立即显示正式消息。
- [x] P1-1 收尾已移除过渡期 `allowMainThreadQueries()`；当前 pending 查询已改为 Room `LiveData` 可观察链路，文本入队/重试/dispatcher DAO 收口统一转到后台线程，repository 合并状态回主线程更新。

##### P1-2. 改造聊天列表数据源：远端 confirmed + 本地 pending 合并展示
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/data/repository/MessageRepositoryImpl.java`
  - [x] 明确拆分两条数据源：
    - [x] 远端 confirmed 消息列表（继续沿用现有分页/缓存）；
    - [x] 本地 pending 列表（来自 pending store）。
  - [x] 新增合并层（当前已收口为 `buildMergedMessages(...)` + `mergedConversations`），统一按会话输出聊天页可观察结果。
  - [x] 合并规则必须稳定：
    - [x] pending 与 confirmed 按时间排序；
    - [x] pending 状态可投影为当前 UI 模型；
    - [x] 不再把 pending 直接塞回远端 `conversations` 原始缓存源。
  - [x] 明确去重策略：
    - [x] 优先使用 `clientRequestId` / `localId -> serverMessageId` 映射；
    - [x] 服务端正式消息出现后，只保留一条逻辑消息。
- [x] `thunder-note-android/app/src/main/java/com/flashnote/java/ui/chat/MessageAdapter.java`
  - [x] 在尽量不改主体结构的前提下，补齐 pending 渲染：
    - [x] 发送中 / 上传中；
    - [x] 失败；
    - [x] 重试入口；
    - [x] 禁止预加载本地未上传完成的远端资源。

> **完成快照（2026-03-25）**
> - Android `PendingMessage.clientRequestId` 已贯穿到服务端 `messages.client_request_id` 字段，`send/list` 正式消息模型现会回传 `clientRequestId`，P1-2 所需的稳定映射键已打通。
> - `MessageRepositoryImpl` merged layer 现优先按 `clientRequestId` 去重，其次按 `serverMessageId` 去重；仅在旧数据或契约缺口场景下，才落回保守文本指纹兜底。
> - `MessageAdapter` 已补本地路径预加载保护，避免未上传媒体把本地临时路径误走远端 URL 解析。
> - 当前 **P1-2 已完整完成**；后续媒体 pending 的 upload→send 双阶段状态机与 `ChatFragment` 媒体入口彻底 enqueue 化，归入 P1-4 继续推进。

**QA：**
- [x] 本地 pending 与远端正式消息不会重复两条。
- [x] 文本 pending 成功后能从 `SENDING` 平滑切到正式消息。
- [x] 列表刷新/重新 bind 不会把 pending 视图抹掉。

##### P1-3. 建立最小 outbox 执行器（先支持 TEXT，再扩到媒体）
- [x] 现有 `PendingMessageDispatcher` 已收口为最小 outbox 执行器（无需再新建第二套类）。
- [x] Dispatcher 必须满足：
  - [x] 单线程串行推进，不允许并发推进同一会话的多条 pending；
  - [x] 只能读取 pending 行内目标字段，不能依赖 `currentConversationKey` 或当前页面实例；
  - [x] 每一步状态切换后立即落库。
- [x] 最小职责先覆盖：
  - [x] QUEUED -> SENDING（文本消息）
  - [x] SENDING -> SENT / FAILED
- [x] 触发时机先覆盖：
  - [x] 新增 pending 后立即触发一次；
  - [x] 聊天页重新进入时补触发一次；
  - [x] 调试页“立即同步”可补触发一次。

> **完成快照（2026-03-25）**
> - `PendingMessageDispatcher` 现已从“只能按 localId 单条 dispatch”升级为最小 outbox：新增会话级 `dispatchConversation(...)` 与全局 `dispatchAllPending()`，内部仍保持单线程串行执行。
> - `MessageRepositoryImpl.bindFlashNote/bindContact` 在聊天页重进时会自动补触发当前会话的 `QUEUED/FAILED` 文本 pending，避免 pending 只能靠首次发送或手动重试推进。
> - `DebugFragment` 的同步成功回调已补全局 pending 推进入口，便于调试页手动恢复文本 outbox。
> - 当前 **P1-3 已完成 TEXT 主链的最小执行器收口**；媒体 upload→send 双阶段状态机仍归入 P1-4。

**QA：**
- [x] 不依赖定时任务，也能在用户发送文本后尽快执行。
- [x] Fragment 重建 / 切 tab 后再次进入，dispatcher 可继续推进 QUEUED / FAILED 项。

##### P1-4. 媒体消息扩展（先图片/文件，后语音/视频）
- [x] 第一批媒体：`IMAGE` / `FILE`
  - [x] 先写 pending 行：保存 `localFilePath`、`mediaType`、目标会话、基础文件信息。
  - [x] Dispatcher 增加双阶段状态机：
    - [x] `QUEUED -> UPLOADING`
    - [x] `UPLOADING -> UPLOADED`
    - [x] `UPLOADED -> SENDING`
    - [x] `SENDING -> SENT / FAILED`
  - [x] 上传失败后保留单条 `FAILED`，不要直接删掉。
- [x] 第二批媒体：`VOICE` / `VIDEO`
  - [x] 语音：保留时长、文件大小等本地信息。
  - [x] 视频：已落地“压缩 -> 上传 -> 发送”的状态推进顺序。
  - [x] 视频链路已补最小验证：压缩失败、上传失败、发送阶段推进由 dispatcher 主链覆盖。
- [x] `ChatFragment`
  - [x] 图片/文件/拍照入口已只负责采集文件/uri 并调用新的 enqueue 接口。
  - [x] 这三条入口已不再直接 `addLocalMessage/removeLocalMessage` 来维护 pending 生命周期。

> **完成快照（2026-03-25）**
> - `PendingMessage` 已补 `localFilePath / remoteUrl / fileName / fileSize`，图片/文件不再只活在页面内存对象里。
> - `PendingMessageDispatcher` 已接入 `FileRepository`，对 `IMAGE / FILE` 走第一版双阶段状态机：先上传，再发送；上传失败会保留单条 `FAILED` pending。
> - `ChatFragment` 的 `handleImagePicked / handleFilePicked / handleCameraPhoto` 已切到 `enqueueMedia(...)` 主链；页面不再自己串 `addLocalMessage -> upload -> send -> removeLocalMessage`。
> - 因为 `PendingMessage` 新增了持久字段，Android `FlashNoteDatabase` 已从 `version = 1` 升到 `version = 2`；否则旧安装包升级后会因 Room schema 校验失败在进入聊天页时直接闪退。
> - `VOICE` 现已复用 upload→send 主链并持久化 `mediaDuration`；`VIDEO` 现已进入 `PROCESSING -> UPLOADING -> UPLOADED -> SENDING -> SENT/FAILED` 状态机，压缩/上传/发送不再依赖页面内存链路。
> - 回归补丁：`MessageRepositoryImpl` 在 pending 成功回写正式消息时，现会把 `mediaDuration / fileName / fileSize / mediaType / mediaUrl` 等本地元信息继承到 `serverMessage`，避免语音重发成功后时长显示成 `0s`。
> - 回归补丁：`MessageAdapter` 对失败态本地视频/语音已停止继续走播放/播放器打开链路，改为明确提示“发送失败，请重试”，避免离线失败视频点击后只看到“视频地址无效”。
> - 当前 **P1-4 已完整完成**：`IMAGE / FILE / VOICE / VIDEO` 均已进入 pending/dispatcher 主链；失败分类、重试策略和体验细化留给 P1-5 继续收口。

**当前阶段不要做：**
- [ ] 不要把卡片消息一起带进第一版 P1；卡片留到文本/图片/文件/语音/视频都稳定后再单独评估。

**QA：**
- [x] 图片/文件发送后切页回来，pending 仍可见且状态正确。
- [x] 视频压缩失败、上传失败、发送失败均能保留单条失败项，不残留假 uploading。
- [x] 图片/文件/语音/视频成功发送后 pending 与正式消息不重复（沿用 P1-2 merged 去重主链）。

##### P1-5. 建立最小失败恢复与重试策略
- [x] 明确失败分类：
  - [x] 网络错误
  - [x] 本地文件不存在
  - [x] 服务器拒绝
  - [x] 超时
  - [x] （媒体链路）压缩失败
- [x] `PendingMessageRepository`
  - [x] 增加单条重试接口；
  - [x] 增加 `attemptCount` / `lastError` 更新能力；
  - [x] 支持查询某会话下所有 `FAILED` 项。
- [x] `MessageAdapter` / 聊天页
  - [x] 给失败项补单条重试入口；
  - [x] 重试只针对当前失败项，不允许整页全量 refresh 碰运气。
- [x] Dispatcher
  - [x] 重试时按当前状态续跑，不重复创建第二条 pending。
  - [x] 防止双击重试导致并发发送同一条消息。

> **完成快照（2026-03-25）**
> - `PendingMessageDispatcher` 已补最小失败分类：网络错误 / 本地文件不存在 / 服务器拒绝 / 请求超时 / 压缩失败，并统一通过 `errorMessage + attemptCount` 持久化到 pending 行。
> - `PendingMessageRepository` 已补“按会话查询 FAILED 项”能力；聊天页失败消息的重试入口已统一收口为 `retryPendingMessage(localId)`，不再误导为“仅文本重试”。
> - `MessageAdapter -> ChatFragment -> ChatViewModel -> MessageRepositoryImpl` 的单条失败重试链路已打通；重试仅接受 `FAILED` 项，第一次点击会把该项改回 `QUEUED` 并重新 dispatch，后续重复点击不会再为同一条消息创建第二条 pending。
> - 已补定向单测覆盖：本地文件不存在、上传失败、压缩失败、服务端拒绝、FAILED-only 重试入口与非 FAILED 忽略逻辑；当前 `PendingMessageDispatcherTest` + `MessageRepositoryImplTest` 已通过。

**QA：**
- [x] 人为断网后发送文本 / 图片 / 文件，恢复网络后可单条重试成功（当前已由失败分类 + FAILED-only 重试主链 + 定向单测覆盖代码路径；常规手工补证留在 P1-6 回归矩阵统一执行）。
- [x] 本地文件已不存在时，能稳定停留在 FAILED 并给出明确错误（已由 `PendingMessageDispatcherTest.dispatchConversationNow_marksMissingLocalFileAsFailed` 覆盖）。

##### P1-6. 最小回归与风险控制（P1 必做，不允许跳过）
- [ ] 手工回归矩阵：
  - [ ] 文本发送后立刻返回聊天列表再回来
  - [ ] 图片/文件上传后切 tab 再回来
  - [ ] 视频压缩中离开页面再回来
  - [ ] 联系人会话发送 / 闪记会话发送 / 收集箱发送三条主链
  - [ ] Fragment 重建 / 旋转屏 / tab 切换
  - [ ] 失败消息单条重试
- [ ] 代码级风控：
  - [ ] 保持 Feature Flag 或最小开关位（若实现成本可接受），便于快速回退到 P0 路径；
  - [ ] 每个阶段完成后至少跑 `assembleDebug`；
  - [ ] 文本稳定前，不允许把媒体链路全部切到新实现。

#### P1 验收标准
- [ ] 上传中/待发送状态可从本地恢复，不再只存在于内存。
- [ ] 聊天页显示为“远端 + 本地 pending”的合并结果。
- [ ] 成功发送后能去重替换本地项。
- [ ] 失败可单条重试，不靠重新进入页面或全量刷新。
- [ ] 文本主链先稳定，再逐步扩到图片 / 文件 / 语音 / 视频；P1 阶段不因大爆炸改造引入新的聊天闪退/重复消息/串会话问题。

---

### 3.5.5 P2：真正离线优先（本地单一事实源 + 后台同步）

#### P2 目标
- 聊天、上传、闪记列表进入真正的本地优先模式。
- UI 只观察本地数据库；网络层只负责同步与回写。
- 支持跨页面、跨 Activity/Fragment、甚至 APP 重启后的 pending 恢复。

#### P2 任务清单（P1 完成后才能做）

##### P2-1. 定义 Android 本地单一事实源
- [ ] 明确聊天消息、闪记列表、上传队列、收藏列表各自的本地表和关系。
- [ ] 约束：页面层不得直接把远端返回值塞进 UI，当次返回值必须先落本地再显示。
- [ ] 为 `flashnote` / `message` / `favorite` / `asset` / `pending_ops` 建立统一读写边界。

**QA：**
- [ ] 关闭网络后打开已看过会话，仍能从本地看到最近数据。

##### P2-2. 收口列表与详情页为真正本地观察
- [ ] `FlashNoteTabFragment` 改为只观察本地闪记列表。
- [ ] `ChatFragment` 改为只观察本地消息聚合结果。
- [ ] `FavoriteTabFragment` 若依赖消息详情，也改为优先观察本地缓存结果。

**QA：**
- [ ] 页面不再依赖“进入页面立刻拉接口才有内容”。

##### P2-3. 引入后台同步/恢复执行器
- [ ] 评估并接入 WorkManager（或等价后台执行方案）处理：
  - [ ] pending upload 恢复
  - [ ] pending send 恢复
  - [ ] pull 增量同步
  - [ ] push 本地变更
- [ ] 执行器必须以本地状态表为输入，不允许再直接扫内存 `LiveData` 快照。

**QA：**
- [ ] APP 杀进程后重新打开，未完成上传/发送项能恢复状态。

##### P2-4. 重新定义 sync 的职责边界
- [ ] `SyncRepositoryImpl` 从“调试快照 push/pull”升级为真正服务于本地表的同步仓储。
- [ ] 文档上区分：
  - [ ] 手动调试同步入口
  - [ ] 真正后台同步执行链
- [ ] 若后端接口不足，单独列出接口补齐清单，不允许在 Android 端继续堆临时兼容逻辑。

**QA：**
- [ ] 用户不手动点调试按钮，发送/上传/拉取也能自动达成最小闭环。

##### P2-5. 建立完整回归测试清单
- [ ] 补齐核心手工测试矩阵：
  - [ ] 发送文本后退出页面再回来
  - [ ] 大文件上传切后台再回来
  - [ ] 断网发送后恢复网络
  - [ ] tab 切换 / Fragment 重建 / 旋转屏
  - [ ] APP 杀进程后恢复 pending 状态
- [ ] 补齐关键单测 / 集成测试：
  - [ ] pending 去重替换
  - [ ] 列表不先清空
  - [ ] refresh 触发不重复

#### P2 验收标准
- [ ] UI 只观察本地单一事实源。
- [ ] 上传中/待发送项可跨页面、跨重启恢复。
- [ ] 闪记列表与聊天页不依赖生命周期无脑 refresh。
- [ ] sync 成为本地状态驱动的后台能力，而不是调试级补丁。

---

### 3.5.6 新会话执行规则（很重要）
- 后续任何会话如果要处理“聊天空白 / 上传状态丢失 / 闪记列表重复请求”，**必须先阅读本节 3.5，再动代码**。
- 禁止跳过 P0 直接做 P2；否则会在旧的数据流缺陷上继续叠复杂度。
- 若新会话只修局部 bug，也必须先说明该 bug 属于 P0 / P1 / P2 哪一阶段。
- 每完成本专项中的一个子任务，必须同步更新：
  - `docs/完整开发计划.md`
  - `docs/开发测试部署经验库.md`
  - 如涉及 Android 基线事实变化，再更新 `docs/Android客户端Java重启基线.md`

**进展记录（2026-03-18）**
- 已完成一批 Android + 后端联调缺陷修复：头像上传显示链路、合集删除后闪记归属重置、闪记列表文案规则、消息长按外部分享、设置页去箭头/去重复退出入口、聊天上拉历史防跳动、录音波形增强。
- 后端已补文件物理删除闭环：删除单条消息或删除整条闪记时，会同步清理关联图片/视频/语音/文件及缩略图对象，避免 MinIO 持续堆积。
- 本轮验证结果：后端 `mvn test` 全量通过；Android `assembleDebug` 通过；Android `testDebugUnitTest` 与 `lint` 仍有仓库既有失败（非本轮引入），详见《开发测试部署经验库》新增条目。
- 本轮继续修复 8 项问题：合集删除后标签残留（后端 `tags` 支持置空更新）、闪记左滑删除图标覆盖时机、闪记列表最新消息聚合展示、外部分享文件名回归原始名称、录音弹层声纹化、emoji 头像回显历史缓存、简介输入框样式与自适应高度、混合内容消息到底部定位补偿。
- 本轮新增 3 项能力：认证活跃续期（最长 30 天）、联系人列表接口与 Android 联系人 tab、联系人会话（文字/语音/图片/视频/文件）。
- 本轮新增 11 项需求落地：资料编辑整合（头像入口进入昵称/简介/头像保存）、闪记长按菜单（编辑/隐藏/置顶切换）、收集箱虚拟闪记（ID=-1，默认置顶且受限操作）、闪记页顶部 + 与悬浮 + 重构为“快速收集菜单（消息/图片/文件/拍照）”、联系人好友请求链路（请求 tab + 红点 + 接受/拒绝）、联系人滑动删除、联系人搜索与添加状态、闪记搜索结果分组展示（闪记/闪记消息）。
- 本轮补齐验证闭环：后端 `mvn clean compile` + `mvn test` 通过；Android `assembleDebug` + `test` + `lint` 全部通过。

**进展记录（2026-03-18，本次补丁）**
- 已修复收集箱跨用户串读：后端对 `flash_note_id=-1` 追加“发送者=当前用户 且 接收者=当前用户”约束，Android 聊天仓库在每次绑定会话时主动清空并重新拉取第一页，避免旧账号缓存复用。
- 已修复联系人会话左右气泡判定：Android 消息列表改为基于 `senderId == 当前登录 userId` 判定我方/对方，确保对方消息显示左侧白底，我方消息显示右侧。
- 已修复闪记置顶即时生效并补齐样式：本地列表更新后按 `pinned desc + updatedAt desc` 重新排序，置顶项背景改为淡灰色，避免“需切页才生效”的观感。
- 已完成交互改造：闪记长按菜单与悬浮 + 菜单统一为深色卡片样式并移除“取消”按钮；悬浮 + 的“消息”入口改为独立全页编辑（含基础富文本快捷按钮，保存/取消后返回列表）。
- 已完成“我的”资料页重构：移除“编辑简介”入口，改为进入独立“修改个人资料”页面，支持昵称/简介编辑、头像选择与预览后保存并返回。
- 已继续修复 13 项 Android + 后端问题：资料页保存会持久化头像、收集箱会话恢复完整发送/删除/分页/草稿能力、联系人搜索结果弹窗去重、联系人列表补挂起申请与最新消息、好友左侧白底气泡去灰边、闪记列表合集名称改为灰色 `#` 前缀、收藏进入收集箱标题回填为“收集箱”、合集页把收集箱并入“未分类”首位、好友请求空状态改为灰色图标、闪记搜索结果按 `noteMatched` 去除消息级重复闪记、顶部 `+` 略放大、悬浮 `+` 文件图标改为与其他入口一致的线性风格。


**进展记录（2026-03-18，交互与好友请求增强）**
- 已修复闪记列表滑动删除动画：原 `super.onChildDraw` 传 `0f` 导致 item 不滑动、红色背景被遮盖；修正为传 `dX` 后恢复滑动露出红色背景与垃圾桶图标。
- 已新增联系人滑动删除红色背景动画：联系人 item 布局添加 `deleteOverlay` 视图，`onChildDraw` 绘制红色背景与垃圾桶图标，滑动结束后显示删除/取消覆盖层，点击后弹出确认框。
- 后端新增取消好友申请 API：`DELETE /api/users/contacts/request/{requestId}`，允许发送方撤回 PENDING 状态的申请；同时修复 `removeContact` 对 PENDING 状态的处理（原来只处理 ACCEPTED）。
- Android 收藏页改为长按弹出菜单：移除右下角"移除"按钮，改为长按任意区域（空白区/图片/视频/文件/语音）弹出悬浮框，包含"复制"和"删除"选项，样式与聊天消息长按菜单一致。
- 已修复收藏页图片/视频显示：媒体容器改为 `match_parent` 宽 + `wrap_content` 高 + `adjustViewBounds=true`，保持原始比例自适应显示，不再裁剪；Glide 加载改为 `fitCenter`。
- 已修复收藏页长按弹出位置：每个媒体容器（mediaPreviewContainer、fileInfoContainer、voiceInfoContainer）均单独设置 `OnLongClickListener`，传递自身作为锚点定位弹出框。

**进展记录（2026-03-19，本轮闪记卡片修复）**
- 已修复闪记多选合并误报失败：Android 合并回调改为按统一响应 `code=0` / `isSuccess()` 识别成功，不再出现“合并消息失败，Success”但实际已落库的假失败提示。
- 已修复闪记卡片详情页样式与方向信息：详情页顶部改为与现有页面一致的浅色顶栏；卡片 payload 现携带 `senderId/role`，详情页不再把所有子消息强制渲染为固定左侧头像。
- 已修复卡片编辑器多图附件重复预览/丢失：临时文件名改为毫秒时间戳 + UUID，避免同秒多附件落到同一路径；保存按钮在附件处理中禁用，避免复制未完成就提交导致最终卡片内容缺失。
- 已补齐收藏中的闪记卡片消息展示：后端收藏列表透传 composite `payload`，Android 收藏列表按聊天内卡片样式渲染标题、摘要与缩略图网格，并支持点击进入卡片详情。
- 已优化聊天长按菜单窄屏适配：消息悬浮菜单从单行改为两行三列布局，降低低分辨率手机与大字号场景下的横向溢出风险。

**进展记录（2026-03-19，本轮卡片展示与 Markdown 修复）**
- 已修复卡片视频压缩后的方向丢失：Android 视频压缩 remux 现在会保留原始旋转元数据，上传后的视频方向与拍摄方向保持一致。
- 已补齐卡片多文件预览：聊天气泡中的卡片消息对 `FILE` 项改为逐条显示文件图标、文件名与大小，不再只用 `[文件]` 占位。
- 已调整卡片详情页头部：详情页头部改为参考聊天页顶部的浅灰区域 + 居中标题 + 下方细分隔线，标题字号回落到聊天页风格；同时移除消息列表默认分隔线。
- 已补齐 Markdown 渲染：聊天文本消息、卡片详情里的文本项，以及 `.md` 文件预览页，现均按 Markdown 渲染而不是原样显示语法符号。
---

## 四、当前已确认冲突与前置修复项

这些事项不解决，后续开发会持续返工，必须优先纳入计划。

### 4.1 路径冲突
- 文档历史路径：`/api/v1/...`
- 当前真实后端路径：`/api/...`
- Android 远程 API 声明仍存在路径漂移，例如：
  - `FlashNoteApi` 使用 `flashnotes`
  - 后端真实是 `flash-notes`

### 4.2 登录语义冲突
- 当前后端登录字段：`username`
- Android `AuthViewModel` / `AuthRepositoryImpl` 仍按邮箱语义处理

### 4.3 模型冲突
- 后端 `flash_notes`：`title / content / tags / is_deleted`
- Android 闪记：`summary / pinned / hidden / syncStatus`
- 后端 `messages`：`sender_id / receiver_id / content / read_status`
- Android 消息：`flashNoteId / role / imageUrl / favorited`

### 4.4 同步冲突
- 后端有 `bootstrap / pull / push` 基础接口
- Android `SyncRepositoryImpl` 已接最小真实链路，但仍未形成完整离线同步体系
- 后端 `push` 已能处理 notes / collections / messages / favorites，仍缺冲突解决与删除回放

### 4.5 功能面冲突
- Android 已有 Favorite / Collection / Profile 等展示面
- Favorite 已完成消息收藏的最小后端支持；更复杂收藏模型仍未进入当前阶段
- Android 部分页面仍是演示数据，不是实际业务流

---

## 五、阶段 A：MVP 详细计划（重启前文档收口）

## A0. Android Java 客户端基线

### A0-1. 明确 Java 客户端事实
- [x] 输出当前 Java 客户端模块成熟度快照：哪些是真实链路，哪些是 mock/stub/demo
- [x] 输出页面/导航范围快照，供后续开发参考

### A0-2. 明确 Android ↔ 后端契约
- [x] 保留当前联调契约作为参考
- [x] 抽取 Java 客户端必须继承的后端接口事实：路径、登录语义、关键 DTO 冲突
- [x] 明确当前数据模型中哪些字段是本地临时语义，不应直接继承

**进展记录（2026-03-16）**
- 经代码复核，A1-A7 对应的 Android / 后端 MVP 主链已形成真实闭环，当前未发现额外代码侧 MVP 功能缺口。
- A0 缺失项实质是文档化基线未收口，现已在 `docs/Android客户端Java重启基线.md` 中补齐模块成熟度快照、页面/导航范围快照、接口继承事实与本地临时语义说明。
- 后续若再判断 MVP 是否完成，应以 `docs/Android客户端Java重启基线.md` + 真实 repository/controller 为联合事实来源，而不是仅凭页面是否存在判断。

**进展记录（2026-03-13）**
- 已在 Java 客户端脚手架落地首版 UI 壳层：`Splash → Login/Register → Main(底部四 Tab)`。
- 导航实现采用 `Activity + FragmentContainerView + Fragment`，主壳使用 `BottomNavigationView`，并保留 emoji 标签图标与 #07C160 主色视觉锚点。
- 四个底部页签中：`闪记` 已接入现有列表壳，`合集/收藏/我的` 采用占位 Fragment，满足后续按模块逐步替换。

**QA：** 新会话可直接基于文档开始 Java 客户端脚手架与第一批模块建设。

## A1. 统一接口事实与客户端声明

### A1-1. 统一 Android 远程 API 路径
- [x] 修正 `FlashNoteApi` 路径：`flashnotes` → `flash-notes`
- [x] 核对 `MessageApi` 路径与当前后端 `messages` controller 一致性
- [x] 核对 `SyncApi` HTTP 方法：`pull/bootstrap` 从 Android 的 POST 改为匹配后端当前实现

**QA：** Android 远程 API 声明与后端 controller 逐个对照，无路径/方法漂移。

### A1-2. 统一认证语义
- [x] 将 Android 登录流程从“邮箱输入”调整为“用户名登录”或允许兼容用户名输入
- [x] 对齐 `AuthViewModel`、`AuthRepository`、相关 DTO 与后端 `LoginRequest`
- [x] 文案与占位提示同步改成当前真实语义

**QA：** Android 登录表单与后端 `username` 字段一致，真实调用登录接口成功。

## A2. 认证闭环

### A2-1. Android AuthRepository 真实接入
- [x] 新增或接入真实 `AuthApi`
- [x] 实现登录调用 `POST /api/auth/login`
- [x] 实现注册调用 `POST /api/auth/register`
- [x] 实现刷新 token 调用 `POST /api/auth/refresh?refreshToken=...`
- [x] 实现登出调用 `POST /api/auth/logout`
- [x] 用真实返回值写入 `TokenManager`
- [x] 删除 `mock_access_token` / `mock_refresh_token`

**QA：** Android 可真实登录、登出、刷新 token，应用重启后登录态可恢复。

### A2-2. 登录态与启动流程
- [x] 核对 Splash → Login/Register → Main 的真实状态判断
- [x] 统一 token 失效后的回退路径
- [x] 明确登录失败错误提示策略

**QA：** 无 token 时进入登录页；有有效 token 时进入主界面；token 失效时能回退登录。

## A3. 闪记闭环

### A3-1. Android FlashNoteRepository 接真实后端
- [x] `getList()` 对接 `POST /api/flash-notes/list`
- [x] `create()` 对接 `POST /api/flash-notes`
- [x] `update()` 对接 `PUT /api/flash-notes/{id}`
- [x] `delete()` 对接 `DELETE /api/flash-notes/{id}`
- [x] MVP 阶段继续使用纯远程主链，暂不把 Room 拉回主线

**QA：** Android 创建、编辑、删除闪记后，重新进入列表能看到一致结果。

### A3-2. 闪记数据模型统一
- [x] 确认后端先维持 `content` 字段
- [x] Android 列表预览统一直接读取 `content`
- [x] MVP 阶段暂不支持 `pinned / hidden`
- [x] `syncStatus / localUpdatedAt / lastSyncVersion` 暂不进入当前 Java 主链

**QA：** Android 与后端之间存在稳定可解释的字段映射，不再依赖猜测。

## A4. 消息闭环

### A4-1. 明确消息 MVP 模型
- [x] MVP 当前只支持纯文本消息
- [x] 明确统一方式：后端 messages 保留 sender / receiver，并增加 `flash_note_id` / `role` 承接 Android 闪记内对话
- [x] 先最小扩后端 message schema，再让 Android 按 `flashNoteId` 调用真实链路

**QA：** 形成一份明确的消息 MVP 模型说明，后续实现不再反复改语义。

### A4-2. Android MessageRepository 接真实链路
- [x] 对接消息列表拉取
- [x] 对接基础发送接口
- [x] MVP 阶段不保留 Room 作为消息主缓存
- [x] `flashNoteId` 通过后端 `messages.flash_note_id` 承载

**QA：** Android 在一个真实会话流中可发送并重新拉取消息。

## A5. 合集闭环

### A5-1. 建立 Android Collection 数据层
- [x] 新增 `CollectionApi`
- [x] 新增 `CollectionRepository` / `CollectionRepositoryImpl`
- [x] 将 `CollectionViewModel` 从假数据迁移到真实数据源
- [x] 支持基础列表、创建、修改、删除
- [x] Android 当前展示语义已收敛为“合集分类目录 + `flash_notes.tags` 分组闪记”，不再把合集页当作独立 CRUD 操作台

**QA：** Collection 页面默认展示“合集（分类）-闪记”层级；闪记可在创建/编辑时加入某个合集或留空为未分类；合集重命名/删除后闪记归属会同步调整。

## A6. 文件基础闭环

### A6-1. 建立 Android 文件能力
- [x] 新增 `FileApi`
- [x] 新增 `FileRepository`
- [x] 接入 `POST /api/files/upload`
- [x] 接入 `GET /api/files/download?objectName=...`

**QA：** Android 至少能完成一次真实文件上传与下载。

## A7. 最小同步闭环

### A7-1. Android SyncRepository 最小实现
- [x] 接入 `bootstrap`
- [x] 接入 `pull`
- [x] 接入 `push`
- [x] 明确最小待同步数据来源：当前主链中的 notes / collections / messages / favorites payload

**QA：** Android 可至少执行一次 bootstrap、一次 pull、一次 push，并看到后端返回结果。

### A7-2. 后端 SyncService 最小可用化
- [x] `push()` 不再只是回显 payload
- [x] 至少处理当前 MVP 需要的闪记/合集/基础消息/收藏同步输入
- [x] 明确 `serverTime`、返回结构、出错语义

**QA：** `push` 对真实 payload 有处理结果，不再只是 `{ accepted: true, receivedPayload: ... }`。

---

## 六、阶段 B：增强版计划

## B1. 收藏能力
- [x] 后端新增 favorites 数据模型
- [x] 后端新增收藏/取消收藏/收藏列表接口
- [x] Android 新增 FavoriteRepository
- [x] FavoriteViewModel 从假数据切到真实链路

## B2. 多媒体消息基础版
- [ ] 定义图片消息最小 MVP
- [ ] 明确文件与消息关系模型
- [ ] 接通 Android 图片上传 + 消息发送流程

## B2-2. 复合卡片消息 (阶段目标)
- [x] 后端基础架构：已新增 `COMPOSITE` 消息类型，`messages.payload` 已落地为 JSONB 字段。
- [x] 后端基础架构：已定义统一的卡片消息 JSON 结构及对应的 Java DTO / Android Model（`CardPayload`、`CardItem`）。
- [x] 后端打包接口：已新增 `/api/messages/merge` 接口，支持按标题 + 消息 ID 深拷贝源消息内容生成打包卡片，并补充来源消息归属/会话校验；同时补齐 `payload` 写入 PostgreSQL `jsonb` 的类型处理器，修复合并时报 `character varying -> jsonb` 的插入异常。
- [x] Android 基础渲染：已支持基于 `payload` 渲染复合卡片摘要与九宫格缩略图。
- [x] Android 消息合并入口：已支持长按弹出“多选”、多选后输入卡片标题、调用 `/api/messages/merge` 并跳转 `CardDetailActivity` 查看内部消息。
- [x] Android 卡片创建入口：聊天页工具栏已新增“卡片”入口；闪记列表页悬浮 `+` 菜单已新增“卡片”入口。
- [x] Android 卡片编辑页：已新增完整卡片编辑页，支持标题、同类型附件（图片/视频/文件，最多 9 个）、拍照补图、附件预览与基础富文本快捷项。

## B3. 同步可靠性增强
- [ ] 增加本地待同步队列
- [ ] 增加失败重试策略
- [ ] 增加基础冲突提示

## B4. 用户体验补齐
- [x] 完善 Profile 页真实数据链路（含编辑简介字体规范修正与非加粗化）
- [ ] 完善错误提示、空状态、加载状态
- [x] 完善登录态过期处理（活跃续期 + 30天绝对上限）

## B5. 测试与质量基础建设
- [ ] 后端为 auth / flashnote 补最小行为或集成测试
- [x] 后端为 message / favorite / sync 补最小行为测试
- [ ] Android 为 auth / flashnote / chat 补最小 ViewModel / repository 测试
- [ ] 建立联调检查清单，覆盖登录、闪记、消息、合集、同步
- [ ] 建立文档与代码一致性检查规则

---

## 七、阶段 C：完整项目计划

## C1. 搜索能力
- [ ] 确定 PostgreSQL tsvector 方案（当前使用 LIKE 查询实现基础搜索）
- [ ] 建表/加索引/写查询接口（当前使用基础 LIKE 查询）
- [x] Android 搜索入口与结果页接通（2026-03 实现）

## C2. 完整同步协议
- [ ] 设计客户端操作幂等模型
- [ ] 设计版本号 / 游标体系
- [ ] 设计冲突检测与解决
- [ ] 设计 SSE 与 pull 配合机制

## C3. 内容模型扩展
- [ ] 更完整的消息类型
- [ ] 文件与消息/闪记/收藏的关联关系
- [ ] 更丰富的闪记状态与分类能力

## C4. 系统与诊断能力
- [ ] 健康检查接口
- [ ] 系统信息接口
- [ ] 基础审计、日志、异常观测能力（当前已补后端统一 API 请求/响应日志，并暂时关闭 MyBatis SQL stdout；后续仍需补结构化审计字段、链路追踪与分级采样策略）

## C5. 工程化与发布能力
- [ ] 建立 CI 工作流（后端构建、Android 构建、基础检查）
- [ ] 建立基础环境分层（dev / staging / prod）说明
- [ ] 明确配置项、密钥、对象存储、数据库迁移发布流程
- [ ] 建立回归检查与发布验收清单

---

## 八、阶段 D：远期扩展

## D1. Web 用户端
- [ ] 建立 Vue 3 工程
- [ ] 复用当前 API 契约
- [ ] 完成登录、闪记、消息、合集基础能力

## D2. iOS 客户端
- [ ] 建立 SwiftUI 工程
- [ ] 定义与 Android 对齐的数据与同步策略

## D3. 管理后台
- [ ] 管理员登录与权限边界
- [ ] 用户管理
- [ ] 内容管理
- [ ] 系统统计与配置管理

---

## 九、接口级 TODO 清单

## 9.1 认证
- [x] `POST /api/auth/login`：Android 真实接入
- [x] `POST /api/auth/register`：Android 真实接入
- [x] `POST /api/auth/refresh`：Android 真实接入
- [x] `POST /api/auth/logout`：Android 真实接入
- [ ] `POST /api/auth/send-code`：目标态
- [ ] `POST /api/auth/verify-code`：目标态
- [ ] `POST /api/auth/reset-password`：目标态

## 9.2 用户
- [x] `POST /api/users/profile`：Android 真实接入
- [x] `PUT /api/users/profile`：Android 真实接入

## 9.3 闪记
- [x] `POST /api/flash-notes/list`：Android 真实接入
- [x] `POST /api/flash-notes`：Android 真实接入
- [x] `PUT /api/flash-notes/{id}`：Android 真实接入
- [x] `DELETE /api/flash-notes/{id}`：Android 真实接入
- [ ] `POST /api/flash-notes/{id}/hide`：目标态
- [ ] `POST /api/flash-notes/{id}/restore`：目标态
- [ ] `POST /api/flash-notes/{id}/pin`：目标态
- [ ] `POST /api/flash-notes/{id}/unpin`：目标态

## 9.4 消息
- [x] `POST /api/messages/list`：已支持按 `flashNoteId` 拉取并接入 Android 消息页
- [x] `POST /api/messages`：已支持基础文本消息与 `flashNoteId` / `role`
- [x] `DELETE /api/messages/{id}`：已实现删除消息，需认证，只能删除自己发送或收到的消息
- [ ] `GET /api/messages/stream`：增强版接入 SSE
- [ ] `POST /api/messages/text`：目标态
- [ ] `POST /api/messages/image`：目标态
- [ ] `POST /api/messages/audio`：目标态
- [ ] `POST /api/messages/video`：目标态
- [ ] `POST /api/messages/file`：目标态
- [ ] `POST /api/messages/{id}/favorite`：增强版

## 9.5 合集
- [x] `POST /api/collections/list`：Android 真实接入
- [x] `POST /api/collections`：Android 真实接入
- [x] `PUT /api/collections/{id}`：Android 真实接入
- [x] `DELETE /api/collections/{id}`：Android 真实接入
- [x] Android 当前把 `collections` 视为合集目录；闪记归属通过 `flash_notes.tags` 承接，而不是独立关系表

## 9.6 文件
- [x] `POST /api/files/upload`：Android 真实接入
- [x] `GET /api/files/download?objectName=...`：Android 真实接入

## 9.7 同步
- [x] `POST /api/sync/bootstrap`：Android 真实接入
- [x] `POST /api/sync/pull`：Android 真实接入
- [x] `POST /api/sync/push`：最小可用化

## 9.8 搜索 / 收藏 / 系统
- [x] 收藏相关接口：已提供消息收藏最小闭环
- [ ] 搜索接口：完整项目阶段
- [ ] 系统健康/系统信息接口：完整项目阶段

---

## 十、前置依赖与顺序约束

### 必须优先完成
1. 文档/路径/字段统一
2. Android 认证打通
3. FlashNote 与 Sync API 对齐

### 依赖关系
- 没有真实登录态，就不应继续推进 Android 主线联调
- 没有统一的闪记/消息模型，就不应先做复杂同步
- 没有 FileApi / CollectionApi，就不应把多媒体与合集体验当成已完成
- 没有 MVP 同步闭环，就不应直接推进完整冲突系统
- 没有最小测试与回归检查，就不应把增强版能力视为稳定可交付

### 优先级取舍规则
- 如果多个任务同时可做，优先处理会阻塞后续阶段的任务。
- 如果一个任务属于目标态，但当前主线仍未闭环，默认优先当前主线。
- 如果一个任务只是优化体验，但存在路径/模型/接口漂移，默认先修漂移。

---

## 十一、横切任务清单

## 11.1 测试
- [x] 后端 auth 集成测试
- [x] 后端 flashnote 集成测试
- [x] 后端 message 集成测试
- [x] 后端 sync 集成测试
- [x] Android auth repository / viewmodel 测试
- [x] Android flashnote repository / viewmodel 测试
- [ ] Android chat repository / viewmodel 测试

## 11.2 文档与契约治理
- [ ] 所有新增接口先更新 `docs/API接口设计.md`
- [ ] 所有 schema 变更先更新 Flyway，再更新 `docs/数据库设计.md`
- [ ] Android API 声明变更时必须同步核对后端 controller
- [ ] 每完成一个阶段，回写 `需求分析.md`、`架构设计.md`、`TODO事项.md`

## 11.3 发布与运维
- [ ] 梳理本地开发环境要求
- [ ] 补 staging/prod 配置说明
- [ ] 补数据库迁移发布步骤
- [ ] 补 MinIO / Redis / PostgreSQL 基础运维说明
- [ ] 补 Android 构建、签名、分发说明

## 11.4 安全与稳定性
- [ ] JWT 生命周期与刷新策略复核
- [ ] 登录失败、未授权、token 过期统一处理
- [ ] 文件上传类型/大小校验
- [ ] 基础接口频率限制设计
- [ ] 敏感配置与密钥管理策略

---

## 十二、持续维护规则

- 后续新增任务时，先判断其属于哪个阶段，再补到对应模块下。
- 如果某项任务细化到可以独立开发，优先在本文件中追加“接口级 TODO”而不是散落在别处。
- 如果设计变更会影响范围、模型、接口或阶段顺序，必须同步更新本文件。
- 当某项能力真正落地后，应把它从“目标态/待办”移动到“当前已实现/当前部分实现”。
- 如果后续需要更细的执行层拆分，可在本文件基础上再派生迭代计划，但本文件始终是总计划源头。
